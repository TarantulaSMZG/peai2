// In a real application, this file would be replaced by a build process
// with environment-specific values. For this example, please replace
// the placeholder with your actual Google Gemini API key.
export const environment = {
  apiKey: 'YOUR_API_KEY_HERE'
};
